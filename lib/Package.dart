


//class template to create a list  used to parse pakage from tcp server created drom id name and value
class SensorData {
  String name;
  int id;
  double value;
  SensorData({required this.id, required this.name, required this.value});
}